Sphinx documentation.

install sphinx before building.

Graphics use mermaid as a dependency, install it:
https://github.com/mgaitan/sphinxcontrib-mermaid

To generate:
mkdir build

make html

Alternatively:
sphinx-build -b html source build

Note: you must have a version of sphinx newer than 1.4 or you may have the error message
"Extension error:
Could not import extension sphinx.ext.imgmath (exception: No module named imgmath)"

try installing it from source using pip
