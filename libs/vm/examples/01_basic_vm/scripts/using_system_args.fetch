function main()
  var message : String;

  Print("System args: " + toString(System.Argc()));

  for(i in 0:System.Argc() - 1)
    message = toString(i) + " = " + System.Argv(i);
    Print(message); 
  endfor

endfunction