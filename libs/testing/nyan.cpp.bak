int main()
{
	for(auto a: b);
  for (int a : omg)
    for (int b : nyan)
      for (std::size_t j{long_function_name()}; j != std::numeric_limits<std::size_t>::max();
           ++j, std::cout << j << '\n')
        if (a < b)
          call_really_somethin(a, b, j, a + 1, b + 1, j + 1, a + 2, b + 2, j + 2, a + 3, b + 3,
                               j + 3, a + 4, b + 4, j + 4);
        else
          std::cerr << "None\n";
  /* This here is nyan */ if(a > b) std::cout << "meow\n"; /* and it's done */
  for (int a : omg)
    for (int b : nyan)
    {
      for (std::size_t j{long_function_name()}; j != std::numeric_limits<std::size_t>::max();
           ++j, std::cout << j << '\n')
        if (a < b)
          call_really_somethin(a, b, j, a + 1, b + 1, j + 1, a + 2, b + 2, j + 2, a + 3, b + 3,
                               j + 3, a + 4, b + 4, j + 4);
        else if (b < a)
          std::cerr << "So high!\n";
        else
          std::cerr << "None\n";

      Nyan n;
    }
  for (int a : omg)
    for (int b : nyan)
      for (std::size_t j{long_function_name()}; j != std::numeric_limits<std::size_t>::max();
           ++j, std::cout << j << '\n')
        ;
}
